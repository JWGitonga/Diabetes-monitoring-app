<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.ChatFragmentDoc">

    <ListView
        android:id="@+id/chat_list_doc"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:scrollbars="vertical"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:listitem="@layout/chat_list_item" />

    <ImageView
        android:id="@+id/no_chat_iv_doc"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:contentDescription="@string/picture"
        android:src="@drawable/inbox_empty"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.284"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/no_chat_tv_doc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No chats available"
        android:textColor="#575050"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/chat_list_doc"
        app:layout_constraintEnd_toEndOf="@id/no_chat_iv_doc"
        app:layout_constraintHorizontal_bias="0.494"
        app:layout_constraintStart_toStartOf="@id/no_chat_iv_doc"
        app:layout_constraintTop_toBottomOf="@+id/no_chat_iv_doc"
        app:layout_constraintVertical_bias="0.092"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>